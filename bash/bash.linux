# Colors
export LS_COLORS=$LS_COLORS:'di=1;34:ex=1;32:'

# Aliases
alias ls='ls -h --color=auto --group-directories-first'
#alias make='make -j$(nproc)'

# Tmux default session
if [ -z $TMUX ]; then
  tmux attach -t default || tmux new -s default
fi
